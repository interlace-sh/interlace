name: incremental

connections:
  default:
    type: duckdb
    path: "data/incremental.duckdb"

state:
  connection: default
  schema: interlace

models:
  default_schema: public
  default_materialize: table

logging:
  level: INFO
