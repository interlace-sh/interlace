# Development environment overlay — SQLite only, no Postgres required
# Usage: INTERLACE_ENV=dev interlace run

connections:
  default:
    type: duckdb
    path: "data/warehouse.duckdb"
    attach:
      - type: sqlite
        name: legacy
        path: "data/legacy.sqlite"
        read_only: true

logging:
  level: DEBUG
