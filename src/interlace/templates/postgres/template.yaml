# Template metadata (read by `interlace init --list`; not copied into the project).
title: Postgres
description: Incrementally pull from a Postgres source; a bundled docker-compose seeds one (needs Docker).
requires_env: [postgres]
