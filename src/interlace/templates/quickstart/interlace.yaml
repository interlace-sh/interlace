name: __PROJECT_NAME__
default_dialect: duckdb
# The warehouse: a plain DuckDB file. Also accepted: ":memory:", a
# `ducklake:...` catalog (Parquet + SQL catalog, serialised writes for concurrent
# access), or quack:<host>:<port> for a warehouse served by `interlace serve --quack`.
database: .interlace/warehouse.duckdb
