# Template metadata (read by `interlace init --list`; not copied into the project).
title: Quickstart
description: A no-source starter — a SQL seed, a Python model over it, and a SQL rollup with checks.
